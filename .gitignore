# dependências
/node_modules
/.pnp
.pnp.*

# build
/.next/
/out/
/build
next-env.d.ts
*.tsbuildinfo

# ambiente — NUNCA commitar valores reais
.env
.env.local
.env*.local
.env.production

# documentos gerados/enviados em runtime (ficam no volume da VPS)
/generated/
/generated_preview/

# ferramental de agentes de IA — repositório à parte, não é parte do produto
/agents/

# cofre de credenciais — local, nunca versionado
/docs/CREDENCIAIS_SISTEMA.md

# versão Express/EJS pré-migração — vive na história do git (tag v2-legacy)
/_legacy/

# logs e sistema
npm-debug.log*
yarn-error.log*
.DS_Store
*.pem
/coverage
